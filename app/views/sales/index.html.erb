<p id="notice"><%= notice %></p>

<h1>Listando Ventas</h1>

<table>
  <thead>
    <tr>
      <th>Ganancia</th>
      <th>Descripcion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sales.each do |sale| %>
      <tr>
        <td><%= sale.ganancia %></td>
        <td><%= sale.descripcion %></td>
        <td><%= link_to 'Mostrar', sale %></td>
        <td><%= link_to 'Editar', edit_sale_path(sale) %></td>
        <td><%= link_to 'Eliminar', sale, method: :delete, data: { confirm: 'Está Seguro/a?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Venta', new_sale_path %>
